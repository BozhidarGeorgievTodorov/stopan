# Perfil de ejemplo: reconstruir el catálogo SQLite desde el latest del object graph.
kind: metadata-graph

metadata_graph:
  action: import
  passphrase_file: /etc/stopan/metadata.passphrase
  # object_store: /var/lib/stopan/metadata/object_store
  # no_protection: false
