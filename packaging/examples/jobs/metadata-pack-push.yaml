# Perfil de ejemplo: distribuir metadata pack a otros nodos.
# Si pack_in se omite, Stopan crea el pack desde el latest object graph.
kind: metadata-pack

metadata_pack:
  action: push
  passphrase_file: /etc/stopan/metadata.passphrase
  # pack_in: /var/lib/stopan/metadata_packs/pack.stopanmetapack
  # pack_copies: 3
  # strict_pack_copies: false
