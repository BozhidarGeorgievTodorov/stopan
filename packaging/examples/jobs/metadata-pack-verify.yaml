# Perfil de ejemplo: verificar presencia remota de metadata packs.
kind: metadata-pack

metadata_pack:
  action: verify
  verify_all: true
  # owner_id:
  # Alternativa para un pack concreto:
  # verify_all: false
  # pack_hash:
  # show_sources: false
